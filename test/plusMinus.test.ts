import plusMinus from "../src/leetcode/plusMinus";

describe("plusMinus", () => {
  test("plugMins an empty array", () => {
    const result = plusMinus([]);
    expect(result).toEqual([]);
  });
});
